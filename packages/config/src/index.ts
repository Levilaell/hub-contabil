// Zod schemas for per-firm configuration (deadline triggers, AI threshold,
// taxonomy, routing map, departments, status vocabularies). Built in T4.

export const CONFIG_PACKAGE_NAME = '@hub/config';

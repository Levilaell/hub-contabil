// UI copy (pt-BR) for the root screen. Every feature centralizes its copy
// in a module like this one — no pt-BR strings scattered in JSX.
export const copy = {
  appName: 'Hub Contábil',
  tagline: 'Base do produto — fase de fundação (T1)',
  supabaseConnected: 'Conectado ao Supabase Cloud',
  supabaseDisconnected: 'Sem conexão com o Supabase — confira as variáveis em .env.local',
} as const;
